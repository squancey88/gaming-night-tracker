<%= render RecordShowWrapperComponent.new(
  record: @unit_trait,
  title: @unit_trait.to_s,
  menu_options: [
    { 
      text: "Edit",
      link: edit_unit_trait_path(@unit_trait)
    },
    {
      text: "Delete",
      link: unit_trait_path(@unit_trait),
      data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
    }
  ]
) do |component| %>

  <% component.with_box(title: "Details") do %>

    <%= render @unit_trait %>
  <% end %>
<% end %>